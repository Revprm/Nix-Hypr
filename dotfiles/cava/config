# CAVA Configuration - Tokyo Night Theme (Simple)

[general]
framerate = 60
bars = 64
bar_width = 3
bar_spacing = 1

[input]
method = pulse
source = auto

[output]
method = ncurses
channels = mono
mono_option = average

[color]
# A single foreground color from the Tokyo Night palette
foreground = '#7aa2f7'

# The background color is typically set by your terminal's theme
